# --- Core RAG & LLM (Ollama / ChromaDB) ---
langchain
langchain-community
langchain-core
langchain-ollama
langchain-chroma
langchain-text-splitters
chromadb

# --- Document Parsing & Vision (PDF / HWP / OCR) ---
pymupdf
olefile         # New: HWP binary parsing (no popup)
easyocr
opencv-python
pillow

# --- GPU Acceleration (PyTorch CUDA 12.1 recommended) ---
# Note: For GPU support, manually install: 
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch
torchvision
torchaudio

# --- Voice Processing (STT / TTS) ---
faster-whisper
pyaudio
pyttsx3

# --- Utility & Communication ---
numpy
requests
paho-mqtt
pywin32; platform_system == 'Windows'
