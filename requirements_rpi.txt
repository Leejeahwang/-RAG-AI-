# ==========================================
# 라즈베리파이 5 (Raspberry Pi 5) 전용 의존성
# ==========================================
# ⚠️ 주의: pip 설치 전, 터미널에서 아래의 시스템 패키지를 먼저 설치하세요!
# sudo apt update && sudo apt install -y python3-opencv libportaudio2 libopenblas-dev espeak
# ==========================================

# --- Core AI & Framework ---
# 라즈베리파이 환경에서는 별도의 CUDA 인덱스 없이 기본 torch를 사용합니다.
torch
torchaudio
torchvision
numpy
pillow
python-dotenv
prompt-toolkit
colorama

# --- Vision & Sensors ---
ultralytics
# 라즈베리파이 GUI 의존성 충돌을 막기 위해 일반 opencv 대신 headless 버전 사용 권장
opencv-python-headless
psutil

# --- RAG & Data Engine ---
langchain
langchain-community
sentence-transformers
pypdf
python-docx
python-pptx
beautifulsoup4
requests

# [FAISS 주의사항]
# pip로 설치가 안 되거나 빌드 에러가 나면 아래 항목을 지우고, 
# 터미널에서 'sudo apt install libfaiss-dev' 명령어로 시스템에 직접 설치하세요.
faiss-cpu

# --- Voice Processing (STT & TTS) ---
faster-whisper
pygame
pyaudio                 # (위의 시스템 패키지 libportaudio2 가 반드시 필요함)
pyttsx3                 # (위의 시스템 패키지 espeak 에 의존하여 동작함)
